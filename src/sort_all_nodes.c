#include "../push_swap.h"


void push_and_sort_b(t_list **head_a, t_list **head_b)
{
	t_list *check_a;
	t_list *check_b;

	check_a = *head_a;
	check_b = *head_b;

	
}

void push2(t_list **head_a, t_list **head_b)
{
	ft_p(head_a, head_b, 'a');
	ft_p(head_a, head_b, 'a');
}

void five_or_more(t_list **head_a, t_list **head_b, t_num *man)
{
	push2(head_a, head_b);
	while(count_nodes > 3)
	{
		push_and_sort_b(head_a, head_b);
	}
}